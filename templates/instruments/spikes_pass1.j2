Du bist ein Experte für medizinische Ausbildung und bewertest eine pädiatrische Simulationsübung anhand der **Checkliste Gesprächsstruktur SPIKES — Diagnoseübermittlung Diabetes Mellitus Typ 1**.

## ⚠ GESPRÄCHSKONTEXT — BITTE ZUERST LESEN

Das Gespräch findet **NICHT** direkt mit dem Patienten statt.
Der Kliniker (SPEAKER_00) spricht ausschließlich mit einer **Bezugsperson des Patienten** (SPEAKER_01).

- SPEAKER_00 = Kliniker (zu bewerten)
- SPEAKER_01 = Bezugsperson / Angehöriger (nicht zu bewerten)

---

## Bewertungsmaßstab

{{ scoring_preamble }}

---

## PASS 1 — S-Phase (Setting)

⚠ **Phase-Fokus:** {{ phase_focus }}

{% for item in items %}
---

### Item {{ item.id }}: {{ item.name }}

{{ item.description }}

{% for level, text in item.scoring_guidance.items() %}
- **{{ level }}**: {{ text }}
{% endfor %}

{% for note in item.get('evaluation_notes', []) %}
*Hinweis {{ loop.index }}:* {{ note }}
{% endfor %}
{% endfor %}

---

## Diarisiertes Transkript

⚠ **Diarisierungshinweis:** Kurze Ein-Wort-Segmente wie „Genau", „Mhm", „Ja", „Okay" sind häufig falsch zugeordnet. Prüfe immer den Kontext des umgebenden Dialogs.

{{ transcript_text }}

---

## Anweisungen

Bewerte die 5 S-Phase Items. Prüfe das gesamte Transkript — S-Phase Verhalten kann sich über den Gesprächsbeginn hinaus erstrecken.

Antworte AUSSCHLIESSLICH mit einem validen JSON-Objekt (kein Markdown, kein Text davor/danach):

{
  "items": [
{% for item in items %}
    {
      "id": "{{ item.id }}",
      "item_no": {{ loop.index }},
      "phase": "S",
      "name": "{{ item.name }}",
      "type": "standard",
      "rating": <0, 1 oder 2>,
      "justification": "<Begründung>",
      "evidence": ["<[MM:SS] 'Zitat'>"]
    }{% if not loop.last %},{% endif %}
{% endfor %}
  ]
}
